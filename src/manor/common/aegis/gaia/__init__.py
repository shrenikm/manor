"""
Aegis simulation package.

Owns the ``Sim`` Python object (a non-Drake helper that encapsulates a
``MultibodyPlant`` + ``SceneGraph`` + sensors + ``MeshcatVisualizer`` +
internal ``Simulator``) and the ``EnvironmentConfig`` that parametrises
its scene composition.
"""
