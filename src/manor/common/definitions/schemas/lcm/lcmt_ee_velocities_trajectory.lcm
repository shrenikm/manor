package manor.common.definitions.lcmtypes;

struct lcmt_ee_velocities_trajectory {
    lcmt_timestamp_header header;
    int32_t num_steps;
    int32_t num_velocities;
    double times[num_steps];
    double ee_velocities_array[num_steps][num_velocities];
}
