package manor_lcm;

struct eef_positions_trajectory_t {
    timestamp_header_t header;
    int32_t num_steps;
    int32_t num_positions;
    double times[num_steps];
    double eef_positions_array[num_steps][num_positions];
}
