package manor.common.definitions.lcmtypes;

struct lcmt_joint_positions_trajectory {
    lcmt_timestamp_header header;
    int32_t num_steps;
    int32_t num_joints;
    double times[num_steps];
    double joint_positions_array[num_steps][num_joints];
}
