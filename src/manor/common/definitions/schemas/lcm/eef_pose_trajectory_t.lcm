package manor_lcm;

struct eef_pose_trajectory_t {
    timestamp_header_t header;
    int32_t num_steps;
    double times[num_steps];
    double translations_array[num_steps][3];
    double orientations_array[num_steps][4];
}
