package manor_lcm;

struct eef_twist_trajectory_t {
    timestamp_header_t header;
    int32_t num_steps;
    double times[num_steps];
    double linear_array[num_steps][3];
    double angular_array[num_steps][3];
}
