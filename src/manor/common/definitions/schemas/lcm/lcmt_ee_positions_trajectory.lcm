package manor.common.definitions.lcmtypes;

struct lcmt_ee_positions_trajectory {
    lcmt_timestamp_header header;
    int32_t num_steps;
    int32_t num_positions;
    double times[num_steps];
    double ee_positions_array[num_steps][num_positions];
}
